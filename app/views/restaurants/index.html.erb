<h1>The Spoon - Restaurants</h1>
<ul>
  <% @restaurants.each do |restaurant| %>
    <%#             content        , prefix_path  %>
    <%#                              path helper method %>
    <li>
      <%= link_to restaurant.name, restaurant_path(restaurant.id) %>
      [<%= link_to "Delete", restaurant_path(restaurant.id), method: :delete, "data-confirm": "Are you sure?" %>]
    </li>
  <% end %>
</ul>
